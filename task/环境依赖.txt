操作系统： Windows11、macOS或主流64位Linux。

主软件为Node.js20或更高版本，用于逐行读取JSONL、规范化URL、计算crypto摘要、关联source map并导出CSV。代码仅使用Node.js内置模块，无需安装第三方npm包。PowerShell、Windows终端或系统终端可用于解压和执行npm run process。

适合当前上报批次的普通开发电脑CPU与内存即可完成处理，无需GPU。磁盘需容纳Node.js、源码映射和四份报告。执行账户需能读取data、config、source_maps和rules，并能在input_data下创建output。任务在本地完成，不依赖网络服务。入口需设置有限超时，覆盖本题批次处理并避免异常无限等待。
