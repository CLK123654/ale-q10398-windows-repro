总分：100分

1. 交付文件与Node.js入口
分值：12分
标准答案或判定规则：Prompt声明的交付路径均存在，triage_csp_reports.mjs可由Node.js执行，业务CSV可解析且表头完整
关键字段或路径：output/src/triage_csp_reports.mjs；output/reports/normalized_violations.csv；output/reports/source_lookup.csv；output/reports/suppressed_reports.csv；output/reports/alert_rollup.csv
容差及来源：路径取Prompt与csp_triage_policy.json的required_outputs；CSV允许必要引号以及LF或CRLF行尾
失败边界：源码缺失或不可执行，任一业务报告缺失、改名、错层或表头缺少声明字段
可观察证据及定位方式：交付成员清单、Node.js退出状态和CSV表头
客观硬性标准：Prompt声明的路径可读，入口返回0，业务CSV结构完整
通过阈值：全部满足
优先级：Gate项
所属层级：L8运行验证或可执行行为层

2. 逐行容错与压制去向
分值：12分
标准答案或判定规则：每条非空物理行进入normalized_violations.csv或suppressed_reports.csv；损坏JSON记录line_no且reason为malformed_json，后续合法上报继续处理
关键字段或路径：data/csp_reports.jsonl；suppressed_reports.csv的line_no、report_id、reason、detail；normalized_violations.csv的report_id
容差及来源：物理行号为整数且容差为0；空行不计；解析行为取JSON.parse
失败边界：损坏行终止批次、非空行没有去向、同一行进入两个结果或line_no错位
可观察证据及定位方式：按JSONL物理行逐条关联两张CSV
客观硬性标准：无法解析的非空行以对应line_no进入malformed_json，其后合法上报继续处理，每条非空行只有一个去向
通过阈值：全部非空行闭合
优先级：Gate项
所属层级：L4数据清洗与质量控制层

3. 策略压制次序
分值：15分
标准答案或判定规则：对每条上报按suppression_reason_order依次评估时间窗口、发布版本、disposition、directive、允许域名和重复指纹，取首个命中原因
关键字段或路径：csp_triage_policy.json的report_start_utc、report_end_utc、ignored_directives、allowed_blocked_hosts、suppression_reason_order；release_artifacts.json；suppressed_reports.csv
容差及来源：时间窗口左闭右开，UTC时间精确比较；原因枚举精确比较
失败边界：原因次序写死且不读取策略、旧发布继续定位、report-only或已允许域名进入告警、窗口边界错误
可观察证据及定位方式：逐条重放策略条件并按line_no核对压制表
客观硬性标准：每条压制记录的reason和detail都能从上报、发布索引及策略顺序复算
通过阈值：所有压制行原因正确
优先级：高权重项
所属层级：L6领域计算与专业规则层

4. URL规范化与动态指纹
分值：17分
标准答案或判定规则：未被业务条件压制的enforce事件先规范化URL，再按策略指定的字段次序、分隔符、摘要算法和长度计算指纹；重复项指向相同指纹中最早保留的report_id
关键字段或路径：normalized_violations.csv的report_id、fingerprint、document_path、blocked_host_or_token；suppressed_reports.csv；duplicate_fingerprint_fields；fingerprint_digest
容差及来源：摘要按策略算法、字段次序、分隔符及hex_chars精确复算
失败边界：查询串进入路径、host提取错误、按report_id去重、指纹参数写死或重复项进入主流程
可观察证据及定位方式：从原始上报和策略独立计算每个指纹
客观硬性标准：每条保留事件的URL字段和指纹均可从原始上报与策略复算，重复事件不进入主流程且detail指向首次保留事件
通过阈值：主流程与重复裁决全部正确
优先级：高权重项
所属层级：L5多源关联与字段一致性层

5. Source map定位
分值：20分
标准答案或判定规则：每个保留事件按release_id、source_file、generated_line和generated_column命中映射，source_path、源码行列、function_name与symbol均来自对应map
关键字段或路径：release_artifacts.json；source_maps目录；source_lookup.csv全部字段
容差及来源：生成行列和源码行列均为整数，容差为0；文本精确比较
失败边界：漏映射、错release、只按文件名匹配、行列偏移、源码字段为空或被压制事件进入定位表
可观察证据及定位方式：为每行构造release_id、source_file、generated_line、generated_column联合键回查source map
客观硬性标准：每条source_lookup记录都能用发布索引与source map联合键复算，源码字段与命中映射一致
通过阈值：全部保留事件都能准确定位
优先级：高权重项
所属层级：L5多源关联与字段一致性层

6. 严重级别、负责人和告警汇总
分值：16分
标准答案或判定规则：severity取severity_by_directive，owner_hint按owner_by_source_prefix最长匹配；alert_rollup每个保留指纹一行，first_report_id和report_count与规范化违例一致
关键字段或路径：normalized_violations.csv；alert_rollup.csv；severity_by_directive；owner_by_source_prefix；default_owner
容差及来源：枚举和负责人精确比较；report_count为整数且容差为0
失败边界：severity写死、短前缀覆盖长前缀、重复项增加计数、汇总指纹缺失或first_report_id错误
可观察证据及定位方式：按fingerprint关联规范化违例与告警汇总，再执行策略映射
客观硬性标准：每个保留指纹的severity和owner_hint可由策略复算，first_report_id与report_count和规范化违例一致
通过阈值：所有告警汇总均能与规范化违例及源码定位闭合
优先级：高权重项
所属层级：L9跨产物一致性与可追溯层

7. 业务报告的主键与排序
分值：8分
标准答案或判定规则：normalized_violations.csv与source_lookup.csv按report_id升序，suppressed_reports.csv按line_no升序，alert_rollup.csv按first_report_id升序；跨表report_id、fingerprint、source_path和owner_hint可关联
关键字段或路径：reports目录业务CSV的表头与业务主键
容差及来源：排序取Prompt；CSV序列化空白不参与比较，字段值和空值语义精确比较
失败边界：主键重复、顺序不稳定、跨表指纹或report_id断裂、源码路径与告警负责人不对应
可观察证据及定位方式：解析业务CSV，按业务主键检查唯一性、排序与连接结果
客观硬性标准：业务报告没有重复主键，规定排序成立，所有主流程事件均能从规范化违例连接到源码定位与告警汇总
通过阈值：全部满足
优先级：普通项
所属层级：L9跨产物一致性与可追溯层
